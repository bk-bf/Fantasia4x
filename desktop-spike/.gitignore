# Throwaway desktop-wrapper spike (DISTRIBUTION Phase A — cross-engine TPS reading).
# Source (main.js, configs, Cargo.toml, README) is kept; heavy build outputs are not.
node_modules/
target/
dist/
gen/

# Tauri's stock placeholder app icons (generated by `tauri init`, only needed for `tauri build`,
# not the `tauri dev` spike). Boilerplate — not worth tracking.
tauri/src-tauri/icons/
