node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# Electron packaging output (electron-builder). The icon source (build-resources/) IS committed.
/dist-electron

# Worktrees
.worktrees/
.claude/worktrees/

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*

# Worktree-local port override (see dev.sh)
.devport

!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Debug logs (generated by gameLogger in dev mode)
.debug/
docs/game/NOTES.md

# Rust / WASM build outputs
spatial-core/target/
spatial-core/pkg/
src/lib/spatial-core-pkg/
sim-core/target/
sim-core/pkg/
src/lib/sim-core-pkg/

# Worktree-local port override (see dev.sh)
.devport

# Per-user MCP config (machine-local paths; see AGENTS.md → Performance Profiling)
.mcp.json

# Local architecture vault (Obsidian/Foam notes; not version-controlled for now)
docs/architecture/vault/
