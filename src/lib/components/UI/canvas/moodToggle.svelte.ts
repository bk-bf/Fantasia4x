// §M Shared open/closed flag for the MOOD pop-up, mirroring `healthToggle`. Flip it once and the
// panel stays on for every selected/hovered pawn until toggled off again.
export const moodToggle = $state({ open: false });
