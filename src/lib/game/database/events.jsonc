// World-event definitions — intentionally empty.
//
// The world-event system is a PLANNED feature (random encounters / opportunities /
// disasters with consequences), not the in-game Chronicle log. See the note in
// `core/Events.ts`. Add `GameEvent` entries here when that system is built.
[]
