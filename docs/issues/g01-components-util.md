---
id: g01-components-util
title: Branch no caller can reach — components/util
status: open
kind: correctness
severity: low
ready: false
origin: audit
rules:
  - G01
files:
  - src/lib/components/util/conditionInfo.ts
  - src/lib/components/util/pawnUtils.ts
symbols:
  - src/lib/components/util/conditionInfo.ts::traitGrantLines#0
  - src/lib/components/util/conditionInfo.ts::persistentSources#0
  - src/lib/components/util/conditionInfo.ts::transientSources#0
  - src/lib/components/util/pawnUtils.ts::describeJob#0
created: 2026-09-04
updated: 2026-09-04
---

# Branch no caller can reach — components/util

> **Related:** [issues/README](README.md) · [tools/audit](../../tools/audit/README.md) · [rule source](../../docs/game/ARCHITECTURE.md)

## What breaks

Rule `G01` — branch no caller can reach — holds in 4 places under `components/util`. Each one is listed below with the evidence the audit required before it would record a fail.

The clearest case: The `*Penalty` branch at src/lib/components/util/conditionInfo.ts:102-104 can never run: no trait in the only trait source has an effects key ending in `Penalty`, and both call sites pass only traits whose `effects` object is empty.

## Evidence

- [`src/lib/components/util/conditionInfo.ts:95`](../../src/lib/components/util/conditionInfo.ts#L95) — The `*Penalty` branch at src/lib/components/util/conditionInfo.ts:102-104 can never run: no trait in the only trait source has an effects key ending in `Penalty`, and both call sites pass only traits whose `effects` object is empty.
  - Branch: `else if (name.endsWith('Penalty') && typeof value === 'number')` at src/lib/components/util/conditionInfo.ts:102, pushing `${GRANT_STAT_ABBR[stat] ?? stat} -${value}` at src/lib/components/util/conditionInfo.ts:104.
  - Guard that cannot hold: `name` is a key of `trait.effects`. `Trait.effects` is declared at src/lib/game/core/types/culture.ts:111-141 with no `*Penalty` member (the only `Penalty` field on Trait is `carryPenalty` at src/lib/game/core/types/culture.ts:107, a sibling of `effects`, not a key inside it, e.g. src/lib/game/database/pawns/traits.jsonc:525 where `"carryPenalty": 0.06` sits beside `"effects": {}`). Every Trait at runtime comes from src/lib/game/database/pawns/traits.jsonc via `export const TRAIT_DATABASE: Trait[] = traitDbData as unknown as Trait[]` at src/lib/game/core/gen/culture.ts:7 (also the source for spawned entities at src/lib/game/services/entity/entitySpawning.ts:776). I parsed that file with node (strip comments/trailing commas, then JSON.parse) over all 467 traits: the complete set of effect keys is charismaBonus, combatMods, constitutionBonus, dexterityBonus, healRate, intelligenceBonus, nightVision, perceptionBonus, stealth, strengthBonus, workQuality, workYield, workSpeed — zero keys ending in `Penalty`. Two tests pin this: src/tests/game/systems/t4WeaponAudit.test.ts:149-153 asserts `no *Penalty key survives in traits.jsonc` equals `[]`, and src/tests/game/systems/statAxisProposal.test.ts:510-517 reports the same migration to signed `*Bonus` values. `grep -rn "Penalty" src --include='*.ts' --include='*.svelte'` finds no code that builds a Trait with a `*Penalty` effect; the only non-test hits are the two display functions themselves (src/lib/components/util/conditionInfo.ts:102 and its duplicate at src/lib/components/pawn/TraitCards.svelte:259) and unrelated `movementPenalty`/`sightPenalty`/`coverPenalty` fields.
  - Call sites: `grep -rn "traitGrantLines" src tools docs` returns exactly two calls plus one import — src/lib/components/util/conditionInfo.ts:355 (`const grants = grantingTrait ? traitGrantLines(grantingTrait) : []`), src/lib/components/pawn/TraitCards.svelte:195 (`condGrants: traitGrantLines(trait)`), and the import at src/lib/components/pawn/TraitCards.svelte:18; no string-key or re-export hit, and no hit under src/tests. `grep -rn "conditionInfo" src` shows no other importer of the module. Neither call site reaches the branch: conditionInfo.ts:354 selects the trait with `t.selfCondition === baseId`, and TraitCards.svelte:176,186-195 only calls it inside the `else if (cond)` arm where `cond = trait.selfCondition ? getTransientConditionDef(trait.selfCondition) : undefined`. Parsing traits.jsonc for every trait with `selfCondition` gives 52 traits (feathered, bounding-gait, cant-settle, werewolf-heritage, … ironclad-mind) and each one has `"effects": {}` — so at both call sites `Object.entries(trait.effects ?? {})` is empty and the loop body at src/lib/components/util/conditionInfo.ts:98-127 never executes at all, the `Penalty` branch included.
- [`src/lib/components/util/conditionInfo.ts:180`](../../src/lib/components/util/conditionInfo.ts#L180) — The `case 'blood_loss'` and `case 'shock'` arms of the `switch (def.id)` can never run, because no persistent condition in `conditions.jsonc` carries either id and the sole call site takes `def` from that file only.
  - Two branches: `case 'blood_loss'` at src/lib/components/util/conditionInfo.ts:208-217 (returns per-injury bleeding lines or `['Recent heavy bleeding']`) and `case 'shock'` at src/lib/components/util/conditionInfo.ts:223-224 (returns `Pain N/100`).
  - The guard is `def.id === 'blood_loss'` / `def.id === 'shock'`. `def` is a `ConditionDef` drawn from `PERSISTENT`, defined at src/lib/components/util/conditionInfo.ts:31-32 as `conditionsData.filter((d) => d.transient !== true)`, whose only data source is `import conditionsData from '$lib/game/database/pawns/conditions.jsonc'` at src/lib/components/util/conditionInfo.ts:10. I dumped every `"id": "..."` in src/lib/game/database/pawns/conditions.jsonc (regex `"id"\s*:\s*"([^"]+)"` over the whole file) and neither `blood_loss` nor `shock` appears; `grep -n '"id": *"blood_loss"|"id": *"shock"' src/lib/game/database/pawns/conditions.jsonc` exits 1. The nearest ids are `pain_shock` (conditions.jsonc:368), `hypovolemia` (conditions.jsonc:429), `anemic_shock`, and the transient `bleeding` (conditions.jsonc:616, `"transient": true` at :620, so excluded from `PERSISTENT` and routed to `transientSources` case `'bleeding'` at src/lib/components/util/conditionInfo.ts:259). `blood_loss` exists in the repo only as a death cause: src/lib/game/core/types/health.ts:189, src/lib/game/systems/PawnStateMachine.ts:515, src/lib/game/services/entity/entityLifecycle.ts:164 and :346. `shock` exists only as an audio clip key: src/lib/audio/manifest.ts:221, referenced by `"audio": "shock"` at conditions.jsonc:369 and :430, plus a test-local trigger literal at src/tests/game/core/conditionGraph.test.ts:66 that never calls this function.
  - Call sites: `grep -rn "persistentSources" src/ tools/ docs/` returns exactly two hits in source — the declaration at src/lib/components/util/conditionInfo.ts:180 and one call at src/lib/components/util/conditionInfo.ts:339 (the remaining hits are this audit's own task files under tools/audit/.ledger/). The symbol is not exported and there is no string-key or re-export use. At line 339 `def` comes from `PERSISTENT.find((d) => d.id === c.id)` at src/lib/components/util/conditionInfo.ts:325, so `def.id` is always an id present in conditions.jsonc with `transient !== true`; `'blood_loss'` and `'shock'` are not among them. Its own callers do not widen this: `getActiveConditionViews` is called at src/lib/components/UI/canvas/selectionCard.ts:557 and :687, src/lib/components/util/statView.ts:77, src/lib/components/pawn/PawnNeeds.svelte:12 and src/lib/components/screens/work/WorkCellTooltip.svelte:101, all passing a live `Pawn`/`Mob`, none supplying a `ConditionDef`. No test, headless scenario or dev route calls `persistentSources` (the grep above covers src/tests).
- [`src/lib/components/util/conditionInfo.ts:234`](../../src/lib/components/util/conditionInfo.ts#L234) — The `case 'eating'` and `case 'sleeping'` branches are unreachable, because the only call site skips every transient condition whose definition sets `hidden: true`, and those two definitions do.
  - Branch: `case 'eating': return ['Currently eating'];` at src/lib/components/util/conditionInfo.ts:250-251, and `case 'sleeping': return ['Currently sleeping'];` at src/lib/components/util/conditionInfo.ts:252-253.
  - Guard that cannot hold: `id === 'eating'` and `id === 'sleeping'`. The sole caller resolves `baseId` against the transient condition table and skips hidden entries — `const def = TRANSIENT.find((d) => d.id === baseId);` at src/lib/components/util/conditionInfo.ts:349 followed by `if (!def || def.hidden) continue;` at src/lib/components/util/conditionInfo.ts:350. `TRANSIENT` is built at src/lib/components/util/conditionInfo.ts:32 from src/lib/game/database/pawns/conditions.jsonc, where the `eating` entry sets `"hidden": true` at src/lib/game/database/pawns/conditions.jsonc:674 (id at :668) and the `sleeping` entry sets `"hidden": true` at src/lib/game/database/pawns/conditions.jsonc:684 (id at :678). `hidden?: boolean` is declared on `TransientConditionDef` at src/lib/game/core/types/health.ts:30. Any entry whose `baseId` is `eating` or `sleeping` hits `continue` at :350 and never reaches line 367, so `id` can never be either value inside this function.
  - Call sites checked: exactly one — src/lib/components/util/conditionInfo.ts:367 (`...transientSources(entity, baseId)`), inside `getActiveConditionViews` (src/lib/components/util/conditionInfo.ts:320). Searches run: `grep -rn "transientSources" src tools docs` returned only src/lib/components/util/conditionInfo.ts:234 (the declaration), src/lib/components/util/conditionInfo.ts:367 (the call), and two audit-harness scratch files that embed this prompt as text (tools/audit/.ledger/worker-2.json:1, tools/audit/.ledger/tmp/task-2.json:1) — not code. No string-key use and no re-export: the function is not in any `export` statement in the file, so no test, headless scenario or dev route can call it directly. Its only exported entry point is `getActiveConditionViews`, whose five call sites all pass a `Pawn` or `Mob` and all inherit the `def.hidden` filter: src/lib/components/UI/canvas/selectionCard.ts:557, src/lib/components/UI/canvas/selectionCard.ts:687, src/lib/components/util/statView.ts:77, src/lib/components/screens/work/WorkCellTooltip.svelte:101, src/lib/components/pawn/PawnNeeds.svelte:12 (found with `grep -rn "getActiveConditionViews" src tools`).
- [`src/lib/components/util/pawnUtils.ts:24`](../../src/lib/components/util/pawnUtils.ts#L24) — The 'light', 'eat' and 'sleep' cases in describeJob cannot run: the only call site renders jobs taken from gameState.jobs, no producer ever writes a job with those three types, and JobService.generateJobs explicitly strips every 'light' job from the array.
  - Branches src/lib/components/util/pawnUtils.ts:38-39 (case 'light' → `light ${job.buildingId ?? 'campfire'}`), :34-35 (case 'eat' → 'eat') and :36-37 (case 'sleep' → 'rest').
  - Guard is the switch discriminant `job.type` equalling 'light', 'eat' or 'sleep'. No code in the repository constructs a Job with any of those three type values: `grep -rn "type: 'eat'|type: 'sleep'|type: 'light'|type: 'refuel'" src` returns only src/lib/game/services/jobs/refuel.ts:31, and `grep -rn "type: " src/lib/game/services/jobs/*.ts` enumerates every job-pool producer — deconstruct.ts:19, craft.ts:112, caretake.ts:158, construct.ts:30, fetch.ts:38, repair.ts:27, rescue.ts:50, fill.ts:160 and :177, refuel.ts:31, plant.ts:75, haul.ts:163, harvest.ts:77 — plus the one manual insertion at src/lib/game/sim/commands.ts:796 (`type: 'fill' as const`). None emits 'light', 'eat' or 'sleep'. For 'light' there is a second, independent block: src/lib/game/services/JobService.ts:110 runs `jobs = jobs.filter((j) => j.type !== 'light');` over the whole array at the end of generateJobs before it is written back to gameState.jobs (JobService.ts:104, :112). src/lib/game/database/pawns/jobs.jsonc lists the job defs the generator loop iterates (harvest, haul, construct, deconstruct, fetch, fill, craft, caretake, rescue, refuel, repair, plant) and contains no light/eat/sleep def. The Job type union at src/lib/game/core/types/jobs.ts:31-46 still permits all three, so this is a stale case list rather than a type error.
  - Call sites: `grep -rn "describeJob" src tools` returns exactly two hits in source — the definition at src/lib/components/util/pawnUtils.ts:24 and the single call at src/lib/components/util/pawnUtils.ts:147 (no string-key or re-export hits; the only other matches are this audit run's own ledger files under tools/audit/.ledger). describeJob is not exported (pawnUtils.ts:24). Its argument at :147 is `nextJob` from getNextAvailableJob (pawnUtils.ts:89-121), whose candidates come only from `gameState.jobs` (pawnUtils.ts:95). getPawnTaskSummary, the only exported wrapper, has two callers: src/lib/components/pawn/PawnOverview.svelte:39 and src/lib/components/screens/work/PopulationOverview.svelte:14, both passing the live store state (`$gameState`), reached from src/lib/components/screens/PawnScreen.svelte:119 and src/lib/components/screens/WorkScreen.svelte:32. `grep -rn "getPawnTaskSummary|pawnUtils" src/tests` returns nothing, so no test, headless scenario or dev route calls it with a fabricated job; src/routes contains no page rendering these components other than the game itself (+page.svelte and the gear-db/api routes). Since gameState.jobs never holds a 'light', 'eat' or 'sleep' job, no path reaches those three cases.

## Why nothing caught it

Nothing below the judgment tier can decide this one: it is why `G01` exists at T2 rather than as a lint rule or a test. The invariant is stated in `docs/game/ARCHITECTURE.md`. If the fix makes the class mechanically checkable, add that check and demote the rule — `node tools/audit/audit.mjs demote` tracks which rules have earned it.

## Remediation

- [ ] Confirm each citation above still holds; drop any whose evidence does not.
- [ ] Fix every remaining site under `components/util` — this is one class, one PR.
- [ ] Add the check that would have caught it, or record why it stays a judgment call.
- [ ] `pnpm check` and `pnpm test:related` on the changed files are green.

## Out of scope

Sites outside `components/util`, and any other rule's findings — they are their own issues. Widening this PR past the citations above makes it unreviewable.
