---
id: s01-components-util
title: Hand-maintained roster restates a declared set — components/util
status: open
kind: drift
severity: high
ready: false
origin: audit
rules:
  - S01
files:
  - src/lib/components/util/combatNarration.ts
  - src/lib/components/util/conditionInfo.ts
  - src/lib/components/util/itemCategoryTree.ts
  - src/lib/components/util/pawnBlurb.ts
  - src/lib/components/util/statView.ts
  - src/lib/components/util/workUtils.ts
symbols:
  - src/lib/components/util/combatNarration.ts::NarrationTier#0
  - src/lib/components/util/combatNarration.ts::TIER_ORDER#0
  - src/lib/components/util/combatNarration.ts::HIT_VERBS#0
  - src/lib/components/util/conditionInfo.ts::MOD_LABEL#0
  - src/lib/components/util/conditionInfo.ts::GRANT_STAT_ABBR#0
  - src/lib/components/util/itemCategoryTree.ts::STATIC_CATEGORY_PATH#0
  - src/lib/components/util/pawnBlurb.ts::STATS#0
  - src/lib/components/util/statView.ts::RES_KEY#0
  - src/lib/components/util/workUtils.ts::LABOR_LABELS#0
  - src/lib/components/util/workUtils.ts::LVL_NAMES#0
  - src/lib/components/util/workUtils.ts::NON_SKILL_TASKS#0
  - src/lib/components/util/workUtils.ts::ABBR#0
created: 2026-09-04
updated: 2026-09-05
---

# Hand-maintained roster restates a declared set — components/util

> **Related:** [issues/README](README.md) · [tools/audit](../../tools/audit/README.md) · [rule source](../../docs/issues/core-stat-single-source.md)

## What breaks

Rule `S01` — hand-maintained roster restates a declared set — holds in 12 places under `components/util`. Each one is listed below with the evidence the audit required before it would record a fail.

The clearest case: `NarrationTier` re-types by hand the four members of the already-declared `WoundSeverity` union, and a fifth severity added there would silently narrate as the lowest tier instead of failing to compile.

## Evidence

- [`src/lib/components/util/combatNarration.ts:3`](../../src/lib/components/util/combatNarration.ts#L3) — `NarrationTier` re-types by hand the four members of the already-declared `WoundSeverity` union, and a fifth severity added there would silently narrate as the lowest tier instead of failing to compile.
  - src/lib/components/util/combatNarration.ts:3 — `export type NarrationTier = 'minor' | 'serious' | 'critical' | 'destroyed';`, with the same four strings written out again at src/lib/components/util/combatNarration.ts:5 (`TIER_ORDER`).
  - The set is declared at src/lib/game/core/defs/wounds.ts:74 — `export type WoundSeverity = 'minor' | 'serious' | 'critical' | 'destroyed';`, produced by src/lib/game/core/defs/wounds.ts:80 `severityFromFrac` and ranked at src/lib/game/core/defs/wounds.ts:86 `SEVERITY_RANK`. The value reaching this symbol is that same severity: src/lib/game/systems/Combat.ts:1348 sets `woundSeverity: result.injury.severity` (injury severity typed at src/lib/game/core/types/health.ts:75) onto `CombatTurnEntry.woundSeverity` (src/lib/game/core/defs/events.ts:36).
  - Add a fifth severity to src/lib/game/core/defs/wounds.ts:74 (say `'maimed'`, returned by `severityFromFrac`) and propagate it through src/lib/game/core/defs/events.ts:36. `NarrationTier` and `TIER_ORDER` stay four long, and the cast at src/lib/components/util/combatNarration.ts:54-56 (`TIER_ORDER.includes(t.woundSeverity as NarrationTier) ? ... : 'minor'`) suppresses any compile error, so `narrationTier` returns `'minor'` for a maiming blow and `describeSwing` picks a verb from the `minor` row of `HIT_VERBS` (src/lib/components/util/combatNarration.ts:9) — the player reads "grazed" for a limb-wrecking hit. No test asserts completeness: src/tests/components/util/combatNarration.test.ts:19-56 exercises `minor`, `serious` and `critical` cases only and never checks that every `WoundSeverity` has a tier.
- [`src/lib/components/util/combatNarration.ts:5`](../../src/lib/components/util/combatNarration.ts#L5) — `TIER_ORDER` hand-writes, in rank order, the four members of the canonical `WoundSeverity` union — which already has an ordered, exhaustiveness-checked rank table — and its `NarrationTier[]` annotation does not force it to stay complete.
  - src/lib/components/util/combatNarration.ts:5 — `const TIER_ORDER: NarrationTier[] = ['minor', 'serious', 'critical', 'destroyed'];`
  - The set is declared at src/lib/game/core/defs/wounds.ts:74 — `export type WoundSeverity = 'minor' | 'serious' | 'critical' | 'destroyed';` — produced by `severityFromFrac` at src/lib/game/core/defs/wounds.ts:80-85 and written onto the combat entry at src/lib/game/systems/Combat.ts:1348 (`woundSeverity: result.injury.severity`), typed again inline at src/lib/game/core/defs/events.ts:36. Its rank order is also already declared exhaustively at src/lib/game/core/defs/wounds.ts:87-91 — `const SEVERITY_RANK: Record<WoundSeverity, number> = { minor: 0, serious: 1, critical: 2, destroyed: 3 }` — which is the same ordering `TIER_ORDER` repeats by hand. `NarrationTier` at src/lib/components/util/combatNarration.ts:3 is itself a hand-copy of that union, so `TIER_ORDER` is not annotated against the canonical set at all.
  - Add a fifth severity to src/lib/game/core/defs/wounds.ts:74 (say `'maimed'`, ranked between `critical` and `destroyed`) and propagate it to src/lib/game/core/defs/events.ts:36. `Record<WoundSeverity, number>` at wounds.ts:87 fails to compile and gets fixed; `const TIER_ORDER: NarrationTier[]` does not, because an array literal missing an element is still assignable to the array type. `TIER_ORDER` stays four long and out of order. Two things then break silently in src/lib/components/util/combatNarration.ts:53-59: `TIER_ORDER.includes(t.woundSeverity as NarrationTier)` is false for `'maimed'`, so `narrationTier` returns `'minor'`; and `bumpTier` (src/lib/components/util/combatNarration.ts:44-47) walks a ladder whose top is `'destroyed'` at index 3, so a crit on a maiming blow steps to the wrong rung. `describeSwing` at src/lib/components/util/combatNarration.ts:86 then reads the `minor` row of `HIT_VERBS` (src/lib/components/util/combatNarration.ts:9,15,21,27) and the combat log at src/lib/components/UI/hud/CombatBreakdown.svelte:3 shows the player "grazed" for a limb-wrecking hit. No test asserts completeness: src/tests/components/util/combatNarration.test.ts:20-56 exercises only `minor`, `serious` and `critical` and never checks that every `WoundSeverity` has a tier (grep `TIER_ORDER` over `src` returns only src/lib/components/util/combatNarration.ts:5,45,46,54).
- [`src/lib/components/util/combatNarration.ts:7`](../../src/lib/components/util/combatNarration.ts#L7) — HIT_VERBS hand-writes four of the six members of the `DamageType` union and is typed `Record<string, …>`, so the two missing families fall back to blunt verbs with no compiler or test complaint.
  - src/lib/components/util/combatNarration.ts:7-32 — `const HIT_VERBS: Record<string, Record<NarrationTier, string[]>>` with exactly four top-level keys: `cutting`, `piercing`, `blunt`, `fire`.
  - The set is declared at src/lib/game/core/types/health.ts:54 — `export type DamageType = 'cutting' | 'piercing' | 'blunt' | 'fire' | 'frost' | 'lightning';`. It is consumed as a closed set elsewhere: src/lib/game/systems/Combat.ts:441 declares `DAMAGE_RESISTANCE_STAT: Record<DamageType, string>` and lists all six, and src/lib/game/core/defs/wounds.ts:63-66 maps every `fromDamageType` including `"frost"` (src/lib/game/database/pawns/wounds.jsonc:88) and `"lightning"` (src/lib/game/database/pawns/wounds.jsonc:99).
  - `frost` and `lightning` are already live, not hypothetical: `grep -n '"damageType"' src/lib/game/database/items/items.jsonc | awk -F'\"' '{print $(NF-1)}' | sort | uniq -c` returns 5 `frost` and 5 `lightning` weapons (e.g. src/lib/game/database/items/items.jsonc:17047 is a topaz staff with `"damageType": "lightning"`). src/lib/components/util/combatNarration.ts:82 does `const family = t.damageType && t.damageType in HIT_VERBS ? t.damageType : 'blunt';`, so a lightning-staff hit is narrated with the blunt verbs at src/lib/components/util/combatNarration.ts:19-24 — the player reads "Wren smashed Goblin #2's left upper leg" for a bolt of lightning, while the same blow produces a frostbite/lightning wound from wounds.jsonc. TypeScript cannot catch this because the key type is `string`, not `DamageType`, and no test asserts completeness: src/tests/components/util/combatNarration.test.ts exercises only `blunt` (line 12), `cutting` (line 29) and `piercing` (line 52). Adding a seventh member to the union at health.ts:54 would extend the gap silently the same way. The only caller is src/lib/components/UI/hud/CombatBreakdown.svelte:3,7 (`grep -rn "describeSwing\|combatNarration" src`), so the wrong verb is what the combat log shows.
- [`src/lib/components/util/conditionInfo.ts:50`](../../src/lib/components/util/conditionInfo.ts#L50) — MOD_LABEL hand-lists the ConditionModifiers fields it will render, and is already short of the interface and of the data file, so several live condition modifiers produce no effect line at all.
  - src/lib/components/util/conditionInfo.ts:50-65 — MOD_LABEL writes out 14 modifier keys by hand.
  - The set is declared at src/lib/game/core/types/health.ts:106-123 (`interface ConditionModifiers`), and used as string keys in src/lib/game/database/pawns/conditions.jsonc. The `Partial<Record<keyof ConditionModifiers, string>>` annotation gives no completeness check, because health.ts:122 adds the index signature `[key: string]: number | undefined`, which widens `keyof ConditionModifiers` to `string | number`. No test asserts completeness: `grep -rn "conditionInfo\|effectLines" src/tests` returns nothing, and `grep -rln "MOD_LABEL" src/tests src/lib` hits only src/lib/components/util/conditionInfo.ts.
  - effectLines (src/lib/components/util/conditionInfo.ts:69) iterates `Object.entries(MOD_LABEL)` only, so any modifier absent from the table renders nowhere. `hygieneRate` is declared at src/lib/game/core/types/health.ts:117 and omitted from MOD_LABEL: the `clean` transient condition at src/lib/game/database/pawns/conditions.jsonc:822 has `"modifiers": {"hygieneRate": 0.0}` as its only modifier, so its `effects` array comes back empty and the player sees a condition with no listed effect, while src/lib/game/services/PawnService.ts:304 and :383 apply that 0.0 multiplier and halt hygiene decay outright. The same holds for `attackSpeed` (src/lib/game/database/pawns/conditions.jsonc:13, :811 and ~40 more stages), `critChance`, `weaponDamage` and `relaxationRate`, all present in the data file and all unlabelled.
- [`src/lib/components/util/conditionInfo.ts:78`](../../src/lib/components/util/conditionInfo.ts#L78) — GRANT_STAT_ABBR hand-lists all six keys of the EntityStats interface, so a seventh stat added there leaves this map short and traitGrantLines falls back to printing the raw stat id.
  - src/lib/components/util/conditionInfo.ts:78-85 — `const GRANT_STAT_ABBR: Record<string, string>` writes out strength, dexterity, intelligence, perception, charisma, constitution by hand; nothing derives it from a source (no Object.keys, no data file).
  - The set is declared at src/lib/game/core/types/culture.ts:3-10 (`interface EntityStats` with exactly those six fields) and named at src/lib/game/core/types/culture.ts:12 (`export type StatKey = keyof EntityStats`). Grep `grep -rn "GRANT_STAT_ABBR\|STAT_ABBR" src/tests` returned nothing, so no test asserts this map covers StatKey.
  - Add a seventh field to EntityStats (say `willpower`) and give a trait `effects: { willpowerBonus: 2 }`: src/lib/components/util/conditionInfo.ts:101 does `GRANT_STAT_ABBR[stat] ?? stat`, the lookup misses, and the condition tooltip built at src/lib/components/util/conditionInfo.ts:355 renders the line `willpower +2` — the raw data key shown to the player instead of an abbreviation. TypeScript cannot catch it because the map is typed `Record<string, string>`, not `Record<StatKey, string>`.
- [`src/lib/components/util/itemCategoryTree.ts:17`](../../src/lib/components/util/itemCategoryTree.ts#L17) — STATIC_CATEGORY_PATH hand-lists 35 item category keys while the real set of categories is whatever `category` values appear in items.jsonc, and it is already short by 17 of them, so those items fall into the 'Other' bucket.
  - src/lib/components/util/itemCategoryTree.ts:17-54 — the hand-written map, consumed at src/lib/components/util/itemCategoryTree.ts:71 (`categoryPath`) and :80 (`categoryKeyPath`) with a `?? ['other']` fallback.
  - The set is declared by the data: `Item.category` is typed only as `category: string` (src/lib/game/core/types/items.ts:167 — no union to enumerate), and the actual members are the `"category"` values in src/lib/game/database/items/items.jsonc. The canonical runtime derivation of that same set already exists at src/lib/game/services/ItemService.ts:293-299 (`getAllCategories()` = `new Set(ITEMS_DATABASE.map(i => i.category))`), and src/lib/components/UI/hud/ResourceSidebar.svelte:70 feeds exactly that list through `categoryKeyPath`.
  - Counting `"category"` values in items.jsonc gives 17 categories with no entry in the map: hide (35 items), reagent (34), cured_hide (17), preserve (14), regalia (8), steel (6), leatherworking (6), binding (6), wool (5), clay (4), woodwork (3), iron (2), plant, ingredient, hygiene, dairy, sweetener (1 each). Concretely, `fleshed_rabbit_hide` (items.jsonc:20194-20205, `"category": "hide"`, not hidden) returns `['other']` from itemCategoryTree.ts:71 and renders under 'Other' (label from CATEGORY_LABELS at itemCategoryTree.ts:120) in ResourceSidebar, ItemFilterChecklist (src/lib/components/UI/canvas/ItemFilterChecklist.svelte:25) and CraftingScreen (src/lib/components/screens/CraftingScreen.svelte:138), instead of Materials → 'Hides & Textiles' where `leather`, `cloth` and `fiber` go. Adding a new category to items.jsonc repeats this silently — nothing errors. No test asserts completeness: src/tests/components/util/itemCategoryTree.test.ts is 62 lines and only checks named single items (weapons, tools, seeds, `spit_meat`); grepping the repo for `STATIC_CATEGORY_PATH` returns only the three lines in this file.
- [`src/lib/components/util/pawnBlurb.ts:4`](../../src/lib/components/util/pawnBlurb.ts#L4) — STATS hand-writes the six members of EntityStats and shadows the canonical StatKey with a local one, so a seventh stat added to the interface would leave this list, and the blurb text it drives, silently short with no type error.
  - src/lib/components/util/pawnBlurb.ts:4-11 — `const STATS = ['strength','dexterity','constitution','intelligence','perception','charisma'] as const;`, followed at line 12 by `type StatKey = (typeof STATS)[number];` which shadows the canonical name for the rest of the file.
  - The set is declared at src/lib/game/core/types/culture.ts:3-10 as `interface EntityStats` with exactly those six numeric fields, and exported as `export type StatKey = keyof EntityStats;` at src/lib/game/core/types/culture.ts:12. `Pawn.stats` — the value this function reads at src/lib/components/util/pawnBlurb.ts:38 — is typed `EntityStats` at src/lib/game/core/types/entities.ts:142 (inside `interface Pawn`, opened at src/lib/game/core/types/entities.ts:135).
  - Add a seventh field to EntityStats (the repo already has src/tests/game/systems/statAxisProposal.test.ts proposing stat-axis changes): `describePawnAbilities` iterates only STATS at src/lib/components/util/pawnBlurb.ts:39, so the new stat is never ranked. A migrant whose only 'mighty' or 'frail' stat is the new one renders an empty strengths/weaknesses list in src/lib/components/UI/modal/MigrantWaveModal.svelte:43, the sole caller (grep for `describePawnAbilities` across src returns exactly src/lib/components/util/pawnBlurb.ts:37 and src/lib/components/UI/modal/MigrantWaveModal.svelte:4,43). Nothing catches it: because `StatKey` is redefined locally at line 12, the `Record<StatKey, …>` tables at src/lib/components/util/pawnBlurb.ts:14 and :23 stay exhaustive against the short list and still compile, and grep for `pawnBlurb|describePawnAbilities|statBucket` across src/tests returns no matches, so no test asserts completeness. The same hand-written six-key roster is duplicated at src/lib/game/services/PawnGrowthService.ts:14, src/lib/game/entities/Pawns.ts:711 and src/lib/components/screens/DebugGodmode.svelte:19.
- [`src/lib/components/util/statView.ts:36`](../../src/lib/components/util/statView.ts#L36) — RES_KEY hand-writes 13 of the numeric trait-effect keys declared on `Trait.effects`, and it is already short by one — `stealth` — so trait contributions to that stat render nowhere.
  - Hand-written list: src/lib/components/util/statView.ts:36-50 (`const RES_KEY: Record<string, string>`), read only at src/lib/components/util/statView.ts:213 (`const rk = RES_KEY[statId]`) inside `traitMods`.
  - The set is declared in two places this table restates: the numeric effect keys on `Trait['effects']` at src/lib/game/core/types/culture.ts:125-141 (`fireResistance` … `nightVision`, plus `stealth` at src/lib/game/core/types/culture.ts:141), and the matching stat ids in src/lib/game/database/pawns/stats.jsonc (heal_rate:101, cutting_resistance:179, piercing_resistance:186, blunt_resistance:193, fire_resistance:200, cold_resistance:207, wetness_resistance:214, lightning_resistance:221, shadow_resistance:228, poison_resistance:235, disease_resistance:242, mental_resistance:249, night_vision:718, stealth:45). A newer resistance roster is declared again at src/lib/game/core/types/culture.ts:78-90 (`resistances?: { cold, fire, poison, disease, mental, lightning, shadow, wetness, cutting, piercing, blunt }`). The inverse of the same table is hand-written a second time at src/lib/components/pawn/TraitCards.svelte:21-35 (`EFFECT_TO_STAT`), so a new key must be added in two files.
  - Concretely, today: the stat `stealth` exists (src/lib/game/database/pawns/stats.jsonc:45), the effect key exists (src/lib/game/core/types/culture.ts:141), and six traits carry it — src/lib/game/database/pawns/traits.jsonc:691, :702, :893, :940, :985, :1291 (values +0.4, -1.5, +0.5, +0.3, +0.3, +0.25). RES_KEY has no `stealth` entry, so at src/lib/components/util/statView.ts:213 `rk` is `undefined`, the block at :214-217 is skipped, and `StatView.traitMods` for `stealth` comes back empty. The pawn stat tooltip shows a stealth value shifted by those traits with no trait named as the source, and nothing throws or warns. The same silence follows for any resistance added to src/lib/game/core/types/culture.ts:78-90 and src/lib/game/database/pawns/stats.jsonc without a matching RES_KEY line. No test asserts this table is complete: `grep -rn "statView\|RES_KEY" src/tests` returns only src/tests/components/aptitudeView.test.ts:2, which imports `computeAptitudeView` and never reaches `traitMods`.
- [`src/lib/components/util/workUtils.ts:19`](../../src/lib/components/util/workUtils.ts#L19) — LABOR_LABELS hand-lists the five labor levels as a `Record<number, string>`, so a sixth member added to the canonical `LaborLevel` union / `LABOR_LEVEL` const would leave it short with no compile error.
  - src/lib/components/util/workUtils.ts:19-25 — `export const LABOR_LABELS: Record<number, string> = { 0: '—', 1: 'LOW', 2: 'NRM', 3: 'HI', 4: 'URG' };` (grep `LABOR_LABELS` over src/ returns only this declaration plus two render sites in src/lib/components/screens/work/WorkPriorities.svelte:9,235,266; no test names it, so completeness is not asserted anywhere)
  - The set is declared at src/lib/game/core/types/jobs.ts:4 `export type LaborLevel = 0 | 1 | 2 | 3 | 4;` and named at src/lib/game/core/types/jobs.ts:5 `export const LABOR_LEVEL = { DISABLED: 0, LOW: 1, NORMAL: 2, HIGH: 3, URGENT: 4 } as const;`
  - Add a level 5 to `LaborLevel` at src/lib/game/core/types/jobs.ts:4 and it flows through src/lib/game/services/WorkService.ts:121-122 (`Record<string, LaborLevel>` defaults) and src/lib/game/sim/commands.ts:545 (`setPawnLaborLevel`) into `laborSettings`. Because the index signature here is `number`, not `LaborLevel`, TypeScript reports nothing; src/lib/components/screens/work/WorkPriorities.svelte:235 then evaluates `LABOR_LABELS[lvl]` to `undefined` and the work-grid cell renders an empty `<span class="cell-lbl">`. The same omission at src/lib/components/util/workUtils.ts:26-32 (`LABOR_COLORS`) leaves the cell uncoloured.
- [`src/lib/components/util/workUtils.ts:33`](../../src/lib/components/util/workUtils.ts#L33) — LVL_NAMES hand-writes one label per member of the LaborLevel union declared in jobs.ts, with no type link and no test, so a sixth level would leave it short and render an empty value.
  - src/lib/components/util/workUtils.ts:33 — `export const LVL_NAMES = ['Off', 'Low', 'Normal', 'High', 'Urgent'] as const;` — five entries written out by hand, positionally indexed by labor level.
  - The set is declared at src/lib/game/core/types/jobs.ts:4 (`export type LaborLevel = 0 | 1 | 2 | 3 | 4;`) and enumerated by name at src/lib/game/core/types/jobs.ts:5 (`export const LABOR_LEVEL = { DISABLED: 0, LOW: 1, NORMAL: 2, HIGH: 3, URGENT: 4 } as const;`). LVL_NAMES is neither typed as `Record<LaborLevel, string>` nor derived from LABOR_LEVEL — it is an inferred `as const` tuple with no reference to either declaration.
  - Add a sixth member (e.g. `FORCED: 5`) at src/lib/game/core/types/jobs.ts:4-5 and LVL_NAMES stays length 5. Nothing fails at compile time: the only consumer is WorkCellTooltip.svelte, whose prop is hand-written as `level: 0 | 1 | 2 | 3 | 4` at src/lib/components/screens/work/WorkCellTooltip.svelte:23 rather than `LaborLevel`, so widening the union never reaches the index expression. At runtime `LVL_NAMES[5]` is `undefined`, and the tooltip's 'Assigned' row at src/lib/components/screens/work/WorkCellTooltip.svelte:146 and :168 renders an empty span for a pawn set to the new level. No test guards it: `grep -rn "LVL_NAMES" src/ docs/` returns only src/lib/components/util/workUtils.ts:33 and src/lib/components/screens/work/WorkCellTooltip.svelte:8,146,168, and `grep -rn "LABOR_LEVEL\|LVL_NAMES\|LABOR_LABELS" src/tests/` returns nothing.
- [`src/lib/components/util/workUtils.ts:35`](../../src/lib/components/util/workUtils.ts#L35) — The keys of NON_SKILL_TASKS hand-restate the membership of NON_SKILL_CATEGORIES, and the work UI uses those keys as its test for "this category has no skill", so a third non-skill category added to the canonical set would be treated as a skilled one by the UI.
  - src/lib/components/util/workUtils.ts:35-49 — `NON_SKILL_TASKS` is typed `Record<string, ...>` and hand-lists exactly two keys, `hunting` (line 36) and `hauling` (line 44); the key set, not just the stat rows, is what the callers read: `if (wc.id in NON_SKILL_TASKS) continue` at src/lib/components/screens/work/WorkPriorities.svelte:137, `for (const id of Object.keys(NON_SKILL_TASKS))` at src/lib/components/screens/work/WorkPriorities.svelte:142, and `NON_SKILL_TASKS[wc.id]` at src/lib/components/screens/work/WorkCellTooltip.svelte:43, which drives the `{#if !nonSkill}` efficiency readout at WorkCellTooltip.svelte:117 and the `{#if nonSkill}` stat block at WorkCellTooltip.svelte:124.
  - The set is declared at src/lib/game/core/rules/body/workExperience.ts:10 — `export const NON_SKILL_CATEGORIES = new Set(['hunting', 'hauling'])` — and is the canonical source: src/lib/game/core/rules/body/workExperience.ts:12-19 builds `SKILL_CATEGORIES` by filtering `WORK_CATEGORIES` (src/lib/game/core/defs/work.ts:3) with `!NON_SKILL_CATEGORIES.has(c.id)`. `NON_SKILL_TASKS` neither imports it nor derives from it; grep `NON_SKILL` over src/ returns only these two declarations plus the three component call sites listed above, and grep `NON_SKILL_TASKS|NON_SKILL_CATEGORIES` over src/tests returns no hits, so no test asserts the two agree.
  - Add a third id to NON_SKILL_CATEGORIES (workExperience.ts:10) — say a future `patrol` — and `SKILL_CATEGORIES` (workExperience.ts:12) drops it, so pawns accumulate no skill level for it. Nothing fails to compile, because the record key type is `string`. In the work matrix, WorkPriorities.svelte:137 no longer skips it, so its skill-derived efficiency (0, since there is no skill) enters `rankWorkCells` alongside the real skill categories and the cell is stamped with a worst-tier `▾` marker; WorkPriorities.svelte:142 does not neutralise its rank. In the tooltip, WorkCellTooltip.svelte:43 yields `undefined`, so WorkCellTooltip.svelte:117 prints a skill-based efficiency percentage for a task that has no skill, and the stat rows at WorkCellTooltip.svelte:124-131 that would explain what actually drives it are omitted.
- [`src/lib/components/util/workUtils.ts:51`](../../src/lib/components/util/workUtils.ts#L51) — ABBR hand-lists 17 work ids against the canonical work roster in `WORK_CATEGORIES`/`SKILL_CATEGORIES`, so it is already 13 entries short and contains one id that no longer exists.
  - src/lib/components/util/workUtils.ts:51-69 — `export const ABBR: Record<string, string>` writes out 17 work ids by hand (foraging…cooking), keyed on nothing derived.
  - The set is declared in src/lib/game/core/defs/work.ts:3-315 (`WORK_CATEGORIES`, 30 ids: foraging, woodcutting, mining, hunting, butchery, fishing, planting, metalworking, tailoring, leatherworking, woodworking, stoneworking, pottery, digging, research, construction, alchemy, caretaking, cooking, weaving, knapping, masonry, lapidary, bonecarving, meals, baking, brewing, herbalism, potions, hauling) and re-derived for the skill grid at src/lib/game/core/rules/body/workExperience.ts:12-19 (`SKILL_CATEGORIES` = WORK_CATEGORIES minus hunting/hauling minus DISCIPLINE_SPLIT_PARENTS, plus DISCIPLINE_LEAVES from src/lib/game/core/defs/disciplines.ts:8-44, whose leaves come from src/lib/game/database/pawns/jobs.jsonc — leatherworking, weaving, knapping, masonry, lapidary, bonecarving, meals, butchery, baking, brewing, herbalism, potions). No test asserts completeness: `grep -rn "ABBR\|abbr" src/tests/` returns nothing.
  - src/lib/components/pawn/PawnSkillBanner.svelte:23 renders every one of the 23 `SKILL_CATEGORIES` as `ABBR[id] ?? id.slice(0, 3).toUpperCase()`. 13 of those ids have no ABBR entry — butchery, planting, caretaking, weaving, knapping, masonry, lapidary, bonecarving, meals, baking, brewing, herbalism, potions — so they fall through to the slice. `potions`.slice(0,3) is `POT`, which is the ABBR entry for `pottery` (src/lib/components/util/workUtils.ts:63): the skill banner shows two different skill cells both labelled `POT`, with different levels and XP bars, and the player cannot tell which is which. src/lib/components/screens/work/WorkPriorities.svelte:67 takes the same fallback for planting, caretaking and hauling. In the other direction the table holds `crafting: 'CRF'` (src/lib/components/util/workUtils.ts:57), which is not a work category id at all — `grep -rn "'crafting'" src/lib/game/core/defs/ src/lib/game/core/rules/` hits only src/lib/game/core/defs/events.ts:47, an event type — so that entry is never read.

## Why nothing caught it

Nothing below the judgment tier can decide this one: it is why `S01` exists at T2 rather than as a lint rule or a test. The invariant is stated in `docs/issues/core-stat-single-source.md`. If the fix makes the class mechanically checkable, add that check and demote the rule — `node tools/audit/audit.mjs demote` tracks which rules have earned it.

## Remediation

- [ ] Confirm each citation above still holds; drop any whose evidence does not.
- [ ] Fix every remaining site under `components/util` — this is one class, one PR.
- [ ] Add the check that would have caught it, or record why it stays a judgment call.
- [ ] `pnpm check` and `pnpm test:related` on the changed files are green.

## Out of scope

Sites outside `components/util`, and any other rule's findings — they are their own issues. Widening this PR past the citations above makes it unreviewable.
