// Test stub for SvelteKit's `$app/environment` so engine modules that read `browser`
// can be imported under Vitest (Node) without the SvelteKit runtime.
export const browser = false;
export const dev = false;
export const building = false;
export const version = 'test';
